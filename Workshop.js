export default class Workshop {
    constructor(workshop) {
        this.id = workshop.id;
        this.name = workshop.name;
        this.notes = workshop.notes;
        this.startDate = workshop.startdate;
        this.endDate = workshop.endDate;
    }
}
